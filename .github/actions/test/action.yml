name: "test"
description: "test"
inputs:
  version:
    description: "the version to use"
    required: false
    default: "stretch-slim"
runs:
  using: "composite"
  steps:
    - run: docker run debian:${{ inputs.version }}
      shell: bash