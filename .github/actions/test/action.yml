name: "test"
description: "test"
inputs:
  version:
    description: "the version to use"
    required: false
    default: "stretch-slim"
runs:
  using: "composite"
  steps:
    - run: docker run debian:${VERSION}
      shell: bash
      env:
        VERSION: ${{ inputs.version }}